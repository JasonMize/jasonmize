import React from 'react'

export const screenWidth = 0
export const screenWidthContext = React.createContext(screenWidth)

export const screenHeight = 0
export const screenHeightContext = React.createContext(screenHeight)